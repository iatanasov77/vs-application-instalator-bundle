<tr data-node-id="{{ item.id }}" {% if parentId %}data-node-pid="{{ item.parent.id }}"{% endif %}>
    <td>{{ item.name }}</td>
    <td>
    	<a href="{{ path('vs_cms_page_categories_update', {'id': item.id}) }}">Edit</a>
    </td>
</tr>

{% for child in item.children %}
	{% include '@VSCms/Page_Categories/partial/simpleTreeTableRows.html.twig' with {'parentId': item.id, 'taxon': child} %}   
{% endfor %}