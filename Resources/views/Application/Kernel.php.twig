<?php namespace App;

use Vankosoft\ApplicationBundle\Component\Application\Kernel as BaseKernel;

class {{ kernelClass }} extends BaseKernel
{
    const VERSION   = '{{ applicationVersion }}';
    const APP_ID    = '{{ applicationSlug }}';
    
    protected function __getConfigDir(): string
    {
        return $this->getProjectDir() . '/config/applications/{{ applicationSlug }}';
    }
}
